import React, { Component } from 'react';

export default class Comparison extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return(
            <p>We did it!</p>
        )
    }
}